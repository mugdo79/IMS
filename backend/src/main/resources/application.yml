spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ims}
    username: ${DB_USERNAME:ims}
    password: ${DB_PASSWORD:ims}
